# Public Supabase client configuration required by the Vite application.
# Set these values in your local .env file and in the deployment platform's environment settings.
# Never commit an environment file containing runtime values.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
